package com.solid.lsp;

public interface IShape {
    // every methods in interface are abstract no need to put abstract notation
    public int getArea();
    public int getWidth();
    public int getHeight();
}